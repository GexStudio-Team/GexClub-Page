export const metadata = {
  title: 'Proyectos',
  description:
    'El vault de GexClub: la prueba de que la excelencia no es una promesa, es un repositorio.',
};

export default function ProjectsLayout({ children }) {
  return children;
}